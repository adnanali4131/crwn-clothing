// we add all the types here and export them
//here the value that point to that exact string

export const UserActionTypes = {
  SET_CURRENT_USER: 'SET_CURRENT_USER',
}

// add this value to the reducer
